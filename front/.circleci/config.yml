version: 2.1

orbs:
  # Note: We are using the latest version of hello-build, whereas module B uses an older one
  front-hello-build: circleci/hello-build@0.0.14

jobs:
  front-build:
    docker:
      - image: alpine
    steps:
      - common-say-hello:
          to: A
      - run: echo 'Hello world from module A! :3'

workflows:
  front-workflow:
    jobs:
      - common-pre
      - front-build:
          requires:
            - common-pre
      - front-hello-build/hello-build:
          requires:
            - common-pre